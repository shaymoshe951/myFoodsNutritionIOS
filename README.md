# MyFoods Nutrition iOS

## TODO

* Auto-prune for debug folder for folders older than 10 days
* Make UI better for add from image/volume
* In add from image: allow adding from free text (without image/volume) so it can still use the AI model with user text input
* Add options to get more than one image (e.g. image of the recipe and image of the food)
* Add option to add the "scanned/AI analyzed" item to the persistent items list (so it can be added directly)
* Merge between AI mode and text search mode (if text search not found ...)
